{
  "name": "@stagecast/infra",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "stagecast": "bin/app.ts"
  },
  "exports": {
    "./render-template": "./lib/render-template.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "synth": "cdk synth",
    "cdk": "cdk",
    "test": "vp test",
    "clean": "rm -rf dist cdk.out *.tsbuildinfo"
  },
  "dependencies": {
    "@stagecast/shared": "workspace:*",
    "aws-cdk-lib": "^2.268.0",
    "constructs": "^10.8.1"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "aws-cdk": "^2.1140.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.13",
    "typescript": "^6.0.3",
    "vitest": "^4.1.0"
  }
}
